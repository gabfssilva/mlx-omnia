[project]
name = "mlx-omnia-workspace"
version = "0.0.0"
requires-python = ">=3.13"
dependencies = ["mlx-omnia", "mlx-omnia-server", "mlx-omnia-cli", "mlx-omnia-application"]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "pytest>=8",
    "hypothesis>=6",
    "ruff>=0.12",
    "pyright>=1.1",
    "transformers>=5.14.1",
    "openai>=1.60",
    "numpy>=2",
    "safetensors>=0.4",
    "huggingface-hub>=0.30",
    "anthropic>=0.40",
    "google-genai>=1.0",
    "import-linter>=2.13",
]

[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
mlx-omnia = { workspace = true }
mlx-omnia-server = { workspace = true }
mlx-omnia-cli = { workspace = true }
mlx-omnia-application = { workspace = true }

[tool.ruff]
line-length = 100
src = ["packages/engine/src", "packages/server/src", "packages/cli/src"]

[tool.ruff.lint]
select = [
    "E", "W",    # pycodestyle
    "F",         # pyflakes
    "I",         # isort
    "UP",        # pyupgrade
    "B",         # bugbear
    "SIM",       # simplify
    "RUF",
]

[tool.importlinter]
root_packages = ["mlx_omnia", "mlx_omnia_application"]

[[tool.importlinter.contracts]]
name = "core and bpe never reach up"
type = "forbidden"
source_modules = ["mlx_omnia.core", "mlx_omnia.bpe", "mlx_omnia.parsers", "mlx_omnia.quant"]
forbidden_modules = ["mlx_omnia.models"]

[[tool.importlinter.contracts]]
name = "model families are independent, except tokenizers"
type = "independence"
modules = ["mlx_omnia.models.*"]
ignore_imports = ["mlx_omnia.models.** -> mlx_omnia.models.*.tokenizer"]

[[tool.importlinter.contracts]]
name = "models never name a dialect"
type = "forbidden"
source_modules = ["mlx_omnia.models"]
forbidden_modules = [
    "mlx_omnia.parsers.qwen",
    "mlx_omnia.parsers.qwen_xml",
    "mlx_omnia.parsers.harmony",
    "mlx_omnia.parsers.atem",
]
allow_indirect_imports = true

[[tool.importlinter.contracts]]
name = "the window stacks one way"
type = "layers"
layers = [
    "mlx_omnia_application.main",
    "mlx_omnia_application.views",
    "mlx_omnia_application.ui",
    "mlx_omnia_application.api",
]

[[tool.importlinter.contracts]]
name = "screens do not know each other"
type = "independence"
modules = ["mlx_omnia_application.views.*"]

[tool.pyright]
typeCheckingMode = "off"
#typeCheckingMode = "strict"
# mlx ships incomplete stubs (mlx.nn has none; array methods take Unknown varargs).
# Our signatures stay fully annotated; these rules only flag mlx's gap.
reportMissingTypeStubs = false
reportUnknownMemberType = false
pythonVersion = "3.13"
include = ["packages"]
exclude = ["**/__pycache__", ".venv", ".legacy", "**/tests/fixtures", "**/build"]
venvPath = "."
venv = ".venv"

[[tool.pyright.executionEnvironments]]
root = "examples"
reportUnknownVariableType = false

# huggingface_hub expõe Unknown nos overloads de download (tqdm genérico).
[[tool.pyright.executionEnvironments]]
root = "packages/server/src"
reportUnknownVariableType = false

# transformers/huggingface_hub (dev-deps de fixture) expõem Unknown; só nos testes.
[[tool.pyright.executionEnvironments]]
root = "packages/server/tests"
reportUnknownMemberType = false
reportUnknownVariableType = false
reportUnknownArgumentType = false
reportPrivateUsage = false

[[tool.pyright.executionEnvironments]]
root = "packages/engine/tests"
reportUnknownMemberType = false
reportUnknownVariableType = false
reportUnknownArgumentType = false
reportPrivateUsage = false

[tool.pytest.ini_options]
testpaths = ["packages/engine/tests", "packages/server/tests", "packages/cli/tests"]
