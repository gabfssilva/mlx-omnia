[project]
name = "sideros-workspace"
version = "0.0.0"
requires-python = ">=3.13"
dependencies = ["sideros", "sideros-server", "sideros-cli"]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "pytest>=8",
    "hypothesis>=6",
    "ruff>=0.12",
    "pyright>=1.1",
    "transformers>=5.14.1",
    "openai>=1.60",
    "numpy>=2",
    "safetensors>=0.4",
    "huggingface-hub>=0.30",
    "anthropic>=0.40",
    "google-genai>=1.0",
    "import-linter>=2.13",
]

[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
sideros = { workspace = true }
sideros-server = { workspace = true }
sideros-cli = { workspace = true }

[tool.ruff]
line-length = 100
src = ["packages/sideros/src", "packages/sideros-server/src", "packages/sideros-cli/src"]

[tool.ruff.lint]
select = [
    "E", "W",    # pycodestyle
    "F",         # pyflakes
    "I",         # isort
    "UP",        # pyupgrade
    "B",         # bugbear
    "SIM",       # simplify
    "RUF",
]

[tool.importlinter]
root_packages = ["sideros"]

[[tool.importlinter.contracts]]
name = "core and bpe never reach up"
type = "forbidden"
source_modules = ["sideros.core", "sideros.bpe", "sideros.tools", "sideros.quant"]
forbidden_modules = ["sideros.models"]

[[tool.importlinter.contracts]]
name = "model families are independent, except tokenizers"
type = "independence"
modules = ["sideros.models.*"]
ignore_imports = ["sideros.models.** -> sideros.models.*.tokenizer"]

[[tool.importlinter.contracts]]
name = "models never name a tool envelope"
type = "forbidden"
source_modules = ["sideros.models"]
forbidden_modules = ["sideros.tools.families"]
allow_indirect_imports = true

[tool.pyright]
typeCheckingMode = "off"
#typeCheckingMode = "strict"
# mlx ships incomplete stubs (mlx.nn has none; array methods take Unknown varargs).
# Our signatures stay fully annotated; these rules only flag mlx's gap.
reportMissingTypeStubs = false
reportUnknownMemberType = false
pythonVersion = "3.13"
include = ["packages"]
exclude = ["**/__pycache__", ".venv", ".legacy", "**/tests/fixtures", "**/build"]
venvPath = "."
venv = ".venv"

[[tool.pyright.executionEnvironments]]
root = "examples"
reportUnknownVariableType = false

# huggingface_hub expõe Unknown nos overloads de download (tqdm genérico).
[[tool.pyright.executionEnvironments]]
root = "packages/sideros-server/src"
reportUnknownVariableType = false

# transformers/huggingface_hub (dev-deps de fixture) expõem Unknown; só nos testes.
[[tool.pyright.executionEnvironments]]
root = "packages/sideros-server/tests"
reportUnknownMemberType = false
reportUnknownVariableType = false
reportUnknownArgumentType = false
reportPrivateUsage = false

[[tool.pyright.executionEnvironments]]
root = "packages/sideros/tests"
reportUnknownMemberType = false
reportUnknownVariableType = false
reportUnknownArgumentType = false
reportPrivateUsage = false

[tool.pytest.ini_options]
testpaths = ["packages/sideros/tests", "packages/sideros-server/tests", "packages/sideros-cli/tests"]
